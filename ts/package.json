{
  "name": "@ompub/rso-verify",
  "version": "0.1.0",
  "description": "TypeScript verifier for the RSO deep-history doc-chain (client #2)",
  "type": "module",
  "license": "MIT",
  "engines": {
    "node": ">=22.6"
  },
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "test": "node --test --experimental-strip-types 'test/*.test.ts'",
    "typecheck": "tsc --noEmit",
    "selftest": "node --experimental-strip-types cmd/rso-verify.ts selftest",
    "spine": "node --experimental-strip-types cmd/rso-verify.ts spine",
    "anchors": "node --experimental-strip-types cmd/rso-verify.ts anchors"
  },
  "devDependencies": {
    "typescript": "^5.4.0",
    "@types/node": "^22.0.0"
  }
}
