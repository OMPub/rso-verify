{
  "name": "@ompub/rso-verify",
  "version": "0.1.0",
  "description": "TypeScript verifier for the RSO deep-history doc-chain (client #2)",
  "type": "module",
  "license": "MIT",
  "engines": {
    "node": ">=24"
  },
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "test": "node --test 'test/*.test.ts'",
    "typecheck": "tsc --noEmit",
    "selftest": "node cmd/rso-verify.ts selftest",
    "spine": "node cmd/rso-verify.ts spine",
    "anchors": "node cmd/rso-verify.ts anchors"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "@types/node": "^24.0.0"
  }
}
